module ActiveAdminHelper 

  # def albums_wrapped_list(scope = Album.hierarchy, deep = '', list = [])
  #   scope.each do |album, hierarchy|
  #     list << [(deep + album.name), album.id]
  #     list += albums_wrapped_list(hierarchy, deep + '-')
  #   end
  #   list
  # end

end